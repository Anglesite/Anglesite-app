<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Anglesite</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSMinimumSystemVersion</key>
	<string>14.0</string>
	<key>NSHumanReadableCopyright</key>
	<string>Copyright © 2026 David W. Keith. ISC License.</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSAppleEventsUsageDescription</key>
	<string>Anglesite needs to coordinate with helper processes (Astro dev server, MCP server) to preview and edit your site.</string>
	<!-- The site preview loads the local Astro dev server over plain http://localhost. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<!-- Sparkle 2.x. Both values are placeholders until the release pipeline is stood up:
	     1. Generate an Ed25519 keypair with `bin/generate_keys` (Sparkle's Tools bundle).
	        Store the private key OUT-OF-REPO in the Keychain (the tool puts it there
	        automatically); paste the *public* key into SUPublicEDKey below.
	     2. Stand up `https://anglesite.dev/appcast.xml` (or a GitHub-Releases-backed
	        equivalent) and replace SUFeedURL.
	     Until both are set with real values, the in-app "Check for Updates…" command
	     will surface a network/signature error to the user. That's intentional — better
	     a clear failure than silently shipping unsigned updates.
	     See docs/release.md for the release pipeline. -->
	<key>SUFeedURL</key>
	<string>https://anglesite.dev/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>REPLACE_WITH_BASE64_ED25519_PUBLIC_KEY</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<key>SUEnableInstallerLauncherService</key>
	<false/>
</dict>
</plist>
